# Core dependencies (match pyproject.toml)
fastmcp>=2.12.4
pydantic>=2.0.0

# Testing dependencies
pytest>=7.4.0
pytest-asyncio>=1.3.0  # Migrated to 1.x (breaking changes fixed in code)
pytest-cov>=7.0.0

# Code quality tools
pre-commit>=3.5.0

# Explicit transitive dependencies to override system packages
# These prevent conflicts with system-installed packages compiled for different Python versions
cffi>=2.0.0  # Required by cryptography, ensures Python 3.11 compatibility
cryptography>=46.0.0  # Required by authlib (via fastmcp), explicit for version control
